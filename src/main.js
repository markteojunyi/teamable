import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
/* this will take the App.Vue file, which has been exported in the App.vue file and bind it to the html page's div id of app */